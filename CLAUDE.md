# Lucas Initiative - 프로젝트 규칙

## 병렬 세션 조율 프로토콜

이 프로젝트는 **여러 Claude Code 세션이 동시에 작업**합니다.
충돌 방지를 위해 아래 규칙을 반드시 따르세요.

### 조율 폴더
`G:\Lucas-Initiative\.coordination\`

### 파일 구조
- `MASTER.md` — **사령탑 전용** (읽기만 하세요). 여기에 지시사항, 아키텍처 결정, 금지사항이 있습니다.
- `worker-dashboard.md` — Dashboard(:7777) 세션 전용 상태 파일
- `worker-scheduler.md` — Scheduler(:7778) 세션 전용 상태 파일
- `worker-*.md` — 추가 워커 세션 상태 파일

### 핵심 규칙

1. **작업 시작 전 반드시 `MASTER.md`를 읽으세요** — 지시사항과 금지사항 확인
2. **자기 상태 파일만 쓰세요** — 남의 파일 수정 금지 (읽기는 OK)
3. **담당 범위 밖의 코드를 건드리지 마세요**
4. **공통 인터페이스(API 스펙, DB 스키마 등) 변경이 필요하면** 자기 상태 파일에 요청을 남기세요. 사령탑이 판단합니다.
5. **작업 상태를 주기적으로 자기 파일에 업데이트하세요**

### 상태 파일 작성 형식

```markdown
## 현재 작업
- [ ] 작업 내용 (진행중)
- [x] 완료된 작업

## 완료 작업
- 무엇을 했는지

## 사령탑에 요청/보고
- 필요한 결정이나 공유사항
```

### 아키텍처 공지: PTY 데몬 분리 (2026-03-01 적용)

Command Center가 **PTY Daemon + Web Server** 2-프로세스 구조로 변경되었습니다.

```
  PTY Daemon (localhost:9100)     Web Server (localhost:9000)
  ┌─────────────────────────┐     ┌──────────────────────────┐
  │ node-pty 세션 관리       │◄TCP►│ Express + Socket.IO      │
  │ 링버퍼 + 파일로그        │     │ REST API + 프론트엔드     │
  │ 장수 프로세스 (절대 안 죽음)│     │ tsx watch OK (자유롭게 재시작)│
  └─────────────────────────┘     └──────────────────────────┘
```

**변경 사항:**
- `server/` 코드 수정 시 웹서버만 재시작됨 → **워커 세션 유지** (컨텍스트 안 날아감)
- `server/daemon/` 코드는 **수정 금지** — 데몬 재시작 시 워커 전체 초기화됨
- 기존 API/Socket.IO 프로토콜 변경 없음 (프론트엔드 영향 0)

**운영:**
```bash
# 터미널 1: 데몬 (장수)
npm run daemon

# 터미널 2: 웹서버 (자유롭게 재시작 가능)
npm run dev
```

### 시스템 사양 (참고)
- CPU: i9-14900KF (24코어/32스레드)
- RAM: 128GB DDR5
- GPU: RTX 4090 (VRAM 24GB)
- Ollama: localhost:11434
