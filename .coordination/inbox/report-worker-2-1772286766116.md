# Report from worker-2
> 2026-02-28T13:52:46.116Z
[Claude 사용량 모니터링 도구 구현 완료]

구현 내역 (4개 파일 생성/수정):

1. server/services/claude-usage-monitor.ts (신규)
   - ~/.claude/.credentials.json에서 OAuth 토큰 자동 읽기
   - 3분 간격 폴링으로 api.anthropic.com/api/oauth/usage 호출
   - five_hour, seven_day, seven_day_opus, seven_day_sonnet 파싱
   - 에러 처리 (토큰 없음, API 실패, 타임아웃)

2. server/routes/api.ts (수정)
   - GET /api/claude-usage — 최신 캐시 데이터 반환
   - POST /api/claude-usage/refresh — 강제 새로고침

3. server/socket/monitor-handler.ts (수정)
   - 'claude-usage' 이벤트로 실시간 브로드캐스트
   - 클라이언트 연결 시 최신 데이터 즉시 전송

4. server/index.ts (수정)
   - claudeUsageMonitor.startPolling(180000) 추가

5. frontend (3개 파일):
   - types/index.ts — ClaudeUsageData, ClaudeUsageWindow 타입 추가
   - hooks/useClaudeUsage.ts — Socket.IO 훅 (신규)
   - components/SystemMonitor.tsx — 사용량 게이지 UI 추가
     - 5h/7d 윈도우 바 (초록→노랑→빨강 색상)
     - 리셋 남은 시간 표시
     - 모델별(Opus/Sonnet) 분리 표시
     - 구독 타입/티어 표시

검증:
- TypeScript 타입 체크 통과 (기존 에러 2개는 coordination-parser.ts에 있던 것, 우리 코드 무관)
- 프론트엔드 빌드 성공 (vite build 완료)

서버 재시작하면 즉시 동작합니다.