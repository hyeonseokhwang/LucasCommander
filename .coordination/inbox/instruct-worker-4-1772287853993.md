# Instruction from Commander
> 2026-02-28T14:10:53.993Z
> To: worker-4

Yoga V2 Backend services setup. Target: G:/Lucas-Initiative/WorkSpace/yoga-v3/server/ ONLY. Step 1: Set up email service - install nodemailer, create server/services/emailService.js. Implement sendMemberApprovalNotification, sendWelcomeEmail functions. Use SMTP config from env vars (SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS). Graceful skip if env vars not set. Step 2: Create .env.example file in yoga-v3/ root documenting ALL required env vars (CLOUDINARY_CLOUD_NAME, CLOUDINARY_API_KEY, CLOUDINARY_API_SECRET, SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, SESSION_SECRET, PORT). Step 3: Wire email service into member approval flow (when admin approves a member, send notification). DO NOT modify NAMASTE_V2/ directory. DO NOT modify rate-limit or validation middleware (worker-2 handles that). Report progress.