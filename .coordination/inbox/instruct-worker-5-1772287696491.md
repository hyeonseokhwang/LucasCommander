# Instruction from Commander
> 2026-02-28T14:08:16.491Z
> To: worker-5

Task assigned: Command Center Dashboard Enhancement. Target: G:/Lucas-Initiative/command-center/ (frontend/ + server/). Implement: (1) Worker Status Card View - visualize each worker current status, PID, last report time, running/stopped indicator. Use GET /api/sessions and GET /api/workers data. (2) Commander Instruction Panel - UI form to select a worker and send instructions via POST /api/instruct. (3) Report/Instruction Timeline - display inbox reports and instruction history chronologically. Use GET /api/reports and inbox files. Build on your previous investigation. Report progress incrementally. Start now.