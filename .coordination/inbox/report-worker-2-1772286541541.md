# Report from worker-2
> 2026-02-28T13:49:01.541Z
> **USER DECISION NEEDED**
[사용량 모니터링 도구 구현 가능성 검토 결과]

결론: 구현 가능. Command Center에 통합하기 매우 적합한 조건.

[검증 완료 사항]
1. OAuth Usage API 실제 호출 성공 - 5시간 사용률 30%, 7일 사용률 4% 실시간 확인
2. 토큰: ~/.claude/.credentials.json 에서 accessToken 추출 가능 (Max $200 플랜, rateLimitTier: default_claude_max_20x)
3. 응답에 five_hour, seven_day, seven_day_opus, seven_day_sonnet 별도 분류 + resets_at 시각 포함

[Command Center 아키텍처 적합성]
- 기술 스택: Node.js + Express + Socket.IO + React (TypeScript)
- 이미 System Monitor(GPU/RAM/CPU/Ollama) 패턴이 존재 - 동일 패턴으로 Claude 사용량 추가 가능
- Socket.IO /monitor 네임스페이스로 실시간 브로드캐스트 구조 확립됨
- REST API 라우터 확장 용이 (routes/api.ts)

[구현 방안 (방법 A: OAuth API 직접 호출)]
1. server/services/claude-usage-monitor.ts 신규 생성
   - credentials.json에서 토큰 읽기 (+ 만료 시 refresh 처리)
   - 3~5분 간격 폴링으로 OAuth Usage API 호출
   - 결과를 메모리에 캐시
2. routes/api.ts에 GET /api/claude-usage 엔드포인트 추가
3. socket/monitor-handler.ts에서 기존 시스템 메트릭과 함께 브로드캐스트
4. frontend: SystemMonitor.tsx에 사용량 게이지 UI 추가

[예상 작업량] 약 4개 파일 수정/생성, 중간 규모 작업

[리스크]
- 비공식 API라 변경/폐쇄 가능 (다만 커뮤니티 도구 다수 사용 중)
- 토큰 만료 시 refresh 로직 필요 (expiresAt 필드 존재)

[요청] 구현 진행 여부 지시 부탁드립니다. 승인 시 즉시 착수 가능합니다.